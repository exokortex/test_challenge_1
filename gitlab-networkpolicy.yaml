apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: gitlab
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/gitlab: "true"
  podSelector:
    matchLabels:
      io.kompose.network/gitlab: "true"
